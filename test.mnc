/: Testing Comments

Does this still work? :/
decs {
	int: a, b, c;
	float: t,r;
	cheese: y,z;
}

t = 1.3;
r = 1.6;
if (1.4 < r)
	shout 1.4, " is smaller than ", r;
else
	shout r, " is smaller than ", 1.4;
end


