decs {
	int: a, b, c;
	cheese: y, r, q;
	float: t, w, n;
	bool: p;
}

y = "Test";
shout y;

listen y;
shout y;

listen y;
shout y;

shout "Type 3 Integers (Each separated by a newline):";
listen a, b, c;
shout a, " ", b, " ", c;
