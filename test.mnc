decs {
	int: a, b, c;
}

a = 0;
b = 0;

shout "Enter A: ";
listen a;

shout a;


shout "Before Loop --------->";

while (b < a)
	shout "B is now:", b;
	b = b + 1;
end

shout "After Loop  --------->";
